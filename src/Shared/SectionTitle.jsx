
const SectionTitle = ({title, subtitle}) => {
    return (
        <>
            <div>
                <p>---------{title}---------</p>
                <h2>{subtitle}</h2>
            </div>
        </>
    );
};

export default SectionTitle;